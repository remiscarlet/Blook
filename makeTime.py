import BeatmapVisualizer

fout = open("temp.txt","wt")

with open("beatmap.txt","rt") as fin:
	for i in xrange(len(fin)):
		line = fin[i]
		

		"yield return new WaitForSeconds(0.0f);"

		makeBlock(x, y);
